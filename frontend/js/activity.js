const activityList=document.getElementById("activityList"); document.getElementById("backButton").addEventListener("click",()=>location.href="dashboard.html"); document.getElementById("refreshButton").addEventListener("click",loadActivity);
function escapeHtml(value){const el=document.createElement("div");el.textContent=value || "";return el.innerHTML;}
function formatDate(value){return value ? new Date(value.replace(" ","T")+"Z").toLocaleString("en-IN",{dateStyle:"medium",timeStyle:"short"}) : "";}
async function loadActivity(){activityList.innerHTML='<div class="empty">Loading...</div>';try{const response=await fetch("/api/production/admin/activity");const entries=await response.json();if(!response.ok)throw new Error(entries.message||"Activity log লোড করা যায়নি");activityList.innerHTML=entries.length?entries.map((entry)=>`<article class="item"><div class="icon"><i class="bi bi-clock-history"></i></div><div><div class="action">${escapeHtml(entry.action)}</div><div class="details">${escapeHtml(entry.details)}</div><div class="date">${escapeHtml(entry.username)} · ${formatDate(entry.created_at)}</div></div></article>`).join(""):'<div class="empty">এখনও কোনো activity নেই।</div>';}catch(error){activityList.innerHTML=`<div class="empty">${escapeHtml(error.message)}</div>`;}}
loadActivity();
