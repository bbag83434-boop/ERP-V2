const backButton = document.getElementById("backButton"); const downloadButton = document.getElementById("downloadButton"); const backupStatus = document.getElementById("backupStatus");
backButton.addEventListener("click", () => location.href = "dashboard.html");
downloadButton.addEventListener("click", async () => { downloadButton.disabled=true; backupStatus.textContent="Backup তৈরি হচ্ছে..."; try { const response=await fetch("/api/production/admin/backup"); const data=await response.json(); if(!response.ok) throw new Error(data.message || "Backup তৈরি করা যায়নি"); const blob=new Blob([JSON.stringify(data,null,2)],{type:"application/json"}); const url=URL.createObjectURL(blob); const link=document.createElement("a"); link.href=url; link.download=`chef-bisu-backup-${new Date().toISOString().slice(0,10)}.json`; document.body.appendChild(link); link.click(); link.remove(); URL.revokeObjectURL(url); backupStatus.textContent="Backup download সম্পন্ন হয়েছে।"; } catch(error) { backupStatus.textContent=error.message; } finally { downloadButton.disabled=false; } });
